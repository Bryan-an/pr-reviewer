/**
 * Stable DOM element IDs for the repos route.
 *
 * Used as both the `id` attribute on the element and the argument
 * to `document.getElementById()` for programmatic scrolling.
 */
export const REPOS_SECTION_ID = "repos-section";
